# PROJECT_LOG.md — Registro cronológico de avances

| Fecha | Cambio | Motivo | Archivos afectados | Riesgos detectados | Próximos pasos |
|---|---|---|---|---|---|
| 2026-05-13 | Análisis inicial de los archivos del proyecto: 3 Excel modelo (Planilla Marangatu, Libro IVA/IVA+IRE, Libro IRP) y PDF "Especificación Técnica para Importación" (junio/2021). | Entender los formatos oficiales exigidos por la SET antes de diseñar el sistema. | `docs/04_ANALISIS_EXCEL.md` (nuevo) | Tablas de códigos de la SET pueden cambiar; deben quedar versionadas en BD. | Validar dudas DQ-001 a DQ-009 con el usuario. |
| 2026-05-13 | Creación del SDD borrador v0.1. | Documentar diseño antes de programar. | `docs/01_SDD.md` (nuevo) | Falta confirmación del usuario sobre alcance multi-usuario y régimen. | Esperar respuesta del usuario y ajustar. |
| 2026-05-13 | Inventario de campos obligatorios y validaciones según Especificación Técnica. | Base para el motor de validación. | `docs/09_VALIDACIONES.md` (nuevo) | Excepciones por tipo de comprobante son numerosas; alto riesgo de olvido. | Crear matriz de excepciones explícita y tests por tipo. |
| 2026-05-13 | Prompt estricto inicial para Gemini con salida JSON estructurada. | Forzar respuestas parseables, evitar alucinación de valores. | `docs/08_PROMPT_GEMINI.md` (nuevo) | Gemini puede ignorar el schema en casos límite; agregar `responseSchema`. | Probar con muestras reales del usuario. |
| 2026-05-13 | Modelo de datos relacional propuesto. | Soportar multi-cliente, auditoría inmutable y catálogos versionados. | `docs/05_MODELO_DATOS.md` (nuevo) | Tablas auxiliares pueden crecer rápido; revisar índices. | Validar con usuario y crear migraciones Prisma. |
| 2026-05-13 | Plan de implementación en 6 fases + backlog inicial. | Acordar orden de entrega y MVP. | `docs/10_PLAN_Y_BACKLOG.md` (nuevo) | Riesgo de scope creep si se cuelan funcionalidades de V2. | Confirmar Fase 1 con usuario. |
| 2026-05-13 | Resolución de DQ-001 a DQ-006 con el usuario. Decisiones D-010..D-014: multi-usuario con roles desde V1, SaaS multi-tenant cloud, soporte de 3 regímenes (IVA, IVA+IRE, IRP), volumen alto con cola Redis, consolidación e-Kuatia en libros internos pero no en ZIP Marangatu. | Estas decisiones cambian alcance y arquitectura del V1. | `docs/02_PROJECT_MEMORY.md`, próximamente `docs/01_SDD.md`, `docs/05_MODELO_DATOS.md`, `docs/07_ARQUITECTURA.md` | Crecimiento de superficie de seguridad (SaaS multi-tenant exige aislamiento estricto). Costos cloud + Gemini para volumen alto. Cumplimiento legal de datos contables en cloud extranjera. | Actualizar SDD, modelo de datos y arquitectura para reflejar SaaS + roles + e-Kuatia. |
| 2026-05-16 | Decisiones de hosting cerradas (DQ-008 parcial): **Supabase** (Postgres dev+prod) + **Vercel** (Next.js) + **Cloudflare R2** (archivos) + **Upstash Redis** (cola). Stack 100% serverless free-tier friendly. | Necesario para arrancar F0 sin Docker local; consolidamos un solo proveedor de DB para evitar duplicar migraciones. | `docs/02_PROJECT_MEMORY.md` (actualizar D-015), `web/.env.example` | Vendor lock-in moderado con Supabase. Costos al escalar volumen Gemini. | Crear cuentas R2/Upstash/Gemini cuando arranque F1/F2. |
| 2026-05-17 | **Cambio de proveedor DB: Supabase → Neon**, manteniendo el resto del stack. Migración `init` y seed de catálogos re-aplicados al nuevo proyecto Neon (`ep-raspy-math-aq39zln5`, us-east-1). | Pedido del usuario. Neon ofrece branching nativo y un free tier más generoso para Postgres puro. | `web/.env.local`, `web/.env.example`, `README.md`, `docs/02_PROJECT_MEMORY.md`, `memory/hosting_stack.md` | Vendor lock-in cambia de Supabase a Neon; mismo orden de magnitud. El proyecto Supabase puede pausarse/borrarse. | Borrar proyecto Supabase cuando esté confirmado que Neon funciona en dev. |
| 2026-05-17 | **Cambio de hosting frontend: Vercel → Fly.io** (web + worker en la misma app, región `gru`). | Pedido del usuario. Fly.io permite correr el worker BullMQ long-running en el mismo deploy que la web Next.js sin partir el stack, cosa que en Vercel obligaba a hostear el worker aparte. | `README.md`, `docs/02_PROJECT_MEMORY.md`, `memory/hosting_stack.md` | Configuración de deploy un poco más manual que Vercel (Dockerfile + fly.toml). Ningún cambio en el código de la app por ahora. | Crear `Dockerfile` y `fly.toml` en Fase 6 (hardening + deploy). Mientras tanto, dev sigue local. |
| 2026-05-17 | **Fase 1 bloque A completado** (lo que no depende de R2): CRUD de clientes con validación RUC+DV módulo 11, baja lógica reversible, restricciones por rol (solo Admin crea/edita/da baja); selector de cliente persistente en cookie HTTP-only con verificación de pertenencia a la org en cada lectura; tests unitarios del validador RUC con vitest (12 tests). CI extendido para correr tests. | Avance Fase 1. Permite que el contador organice sus clientes y deje seleccionado uno antes de cargar comprobantes. | `web/src/app/app/clientes/**`, `web/src/app/app/SelectorCliente.tsx`, `web/src/app/app/selector-cliente-action.ts`, `web/src/lib/cliente-activo.ts`, `web/src/lib/ruc.test.ts`, `web/vitest.config.ts`, `.github/workflows/ci.yml` | Bloque B (upload + visor + listado de comprobantes) requiere credenciales R2; queda pendiente. | Pedir al usuario credenciales R2 y continuar F1-6/7/8. |
| 2026-05-17 | **Naming: el producto se llama "Tavex"** (decidido con el usuario). Rebrand de UI, metadata, README, cookie (`tavex_cliente_activo`), `package.json` (`tavex-web`) y placeholder de bucket R2 (`tavex-archivos`). Se mantienen los slugs de infra existentes (Fly app `marangatu-web`, URL `marangatu-web.fly.dev`) para no romper deploy ni secrets; migración del slug de Fly se hace cuando se quiera URL `tavex.fly.dev`. | Identidad de producto. | `README.md`, `web/README.md`, `web/package.json`, `web/.env.example`, `web/src/app/**`, `web/src/lib/cliente-activo.ts` | Cookie cambió de nombre → si había una sesión con `marangatu_cliente_activo` queda olvidada (usuario tendrá que reseleccionar cliente). No bloqueante. | Cuando se quiera URL bonita, `fly apps move marangatu-web --to tavex` + actualizar AUTH_URL secret. |
| 2026-05-17 | **Adelantar deploy a Fly.io** (originalmente Fase 6) a pedido del usuario para iterar directo en la web sin pasar por localhost. Se preparan `Dockerfile` multi-stage (deps→builder→runner) con Next.js standalone + worker tsx en la misma imagen, `fly.toml` con dos procesos (`web` y `worker`) en región `gru`, y workflow GitHub Actions con job `deploy` que corre tras `build` y lanza `flyctl deploy` en cada push a `main`. `next.config.ts` ahora exporta `output: "standalone"`. | Bajar fricción de feedback (probar la app sin levantar nada localmente). | `web/Dockerfile`, `web/.dockerignore`, `web/fly.toml`, `web/next.config.ts`, `.github/workflows/ci.yml` | El primer `fly launch` debe correrlo el usuario (sigue siendo interactivo). El `FLY_API_TOKEN` debe quedar en secrets de GitHub. Iterar 100% en cloud es 2-5min/cambio vs ~1s en local. | Crear cuenta GitHub repo + Fly.io, configurar secrets, lanzar primer deploy. |
| 2026-05-16 | **Fase 0 completada**. Scaffold Next.js 16 + TS strict + Tailwind v4. Prisma 7 con adapter pg apuntando a Supabase. Schema completo (organización, usuario con rol, cliente, archivo, comprobante con todos los enums, campo_extraido, auditoría, exportación, configuración cifrada, 5 catálogos). Migración aplicada. Seed Tablas 1-5 SET (junio/2021) cargado: 4+2+7+23+2 filas. NextAuth (Auth.js v5 beta) con credenciales + roles ADMIN/OPERADOR + signup que crea organización + admin en transacción. Layout autenticado con selector de cliente (placeholder F1), página de configuración con API Key Gemini cifrada AES-256-GCM. Helpers R2 + BullMQ + worker placeholder. CI GitHub Actions (lint + typecheck + format check). | Cimientos del SaaS listos para arrancar Fase 1. | `web/**` (todo el subproyecto), `.github/workflows/ci.yml`, `README.md`, `.gitignore` | Prisma 7 cambió la sintaxis del datasource (URL ya no en schema); migrado. Next.js 16 cambió `middleware`→`proxy` y obliga a `cookies/headers/params/searchParams` async; respetado. Bcryptjs en vez de argon2 (plan original) para evitar bindings nativos en Windows. | Iniciar Fase 1: CRUD de clientes + carga de comprobantes. |

| 2026-05-18 | **Rename Fly.io a tavex.fly.dev** + fix worker (esbuild en runner). Secrets R2 cargados: Account ID, Access Key, Secret Key, Bucket `tavex-archivos`. | URL de prod ahora es `https://tavex.fly.dev`; `marangatu-web` eliminado. | `web/fly.toml`, `web/Dockerfile`, `web/README.md` | Dockerfile ahora copia node_modules completos al runner (imagen más grande ~290MB). | — |
| 2026-05-18 | **Fase 1 Bloque B completado**: upload drag-and-drop con SHA-256 server-side, deduplicación automática, subida a Cloudflare R2, creación de Archivo + Comprobante en transacción, enqueueing BullMQ (best-effort). Visor PDF/imagen con URL firmada. Listado de comprobantes scoped al cliente activo. | Core de captura de comprobantes operativo. | `web/src/app/api/archivos/upload/route.ts`, `web/src/app/api/archivos/[id]/url/route.ts`, `web/src/app/app/comprobantes/page.tsx`, `web/src/app/app/comprobantes/nuevo/page.tsx`, `web/src/app/app/comprobantes/DropZone.tsx`, `web/src/app/app/comprobantes/VisorArchivo.tsx`, `web/src/app/app/comprobantes/[id]/page.tsx` | Upload proxy (sin presigned PUT) evita necesidad de CORS en R2. | Conectar Gemini para extracción. |
| 2026-05-18 | **Fases F5 + F6 completadas**: Dashboard real con stats por cliente (total mes, pendientes, registrados, rechazados) y tabla de últimos comprobantes. Auditoría filtrable por entidad/usuario/fecha con diff anterior→nuevo. Libro IVA Excel con exceljs (4 hojas, cabeceras fila 10, metadata cliente+año). API libro-iva genera XLSX, lo sube a R2 y crea registro Exportacion. Importación e-Kuatia XML (SIFEN) vía xml2js: mapeo iTipDE→tipoComprobante, crea Comprobante con origen=E_KUATIA_XML y estado=REGISTRADO. | Producto completo del MVP: upload → extracción → revisión → exportación + e-Kuatia. | `web/src/app/app/page.tsx`, `web/src/app/app/auditoria/page.tsx`, `web/src/lib/exportar-excel.ts`, `web/src/app/api/exportaciones/libro-iva/route.ts`, `web/src/app/api/archivos/upload-xml/route.ts`, `web/src/app/app/comprobantes/ImportarXml.tsx`, `web/src/app/app/comprobantes/importar-xml/page.tsx` | exceljs y xml2js agregados como dependencias. | Hardening: tests E2E Playwright, validación formulario revisión, límites de uso. |
| 2026-05-18 | **Fases F2 + F3 + F4 completadas**: Worker BullMQ real con extracción Gemini (prompt estructurado JSON, temperatura 0, todos los campos fiscales PY). Motor de validaciones DNIT V-001..V-022 + K-001. UI de revisión con badges de confianza, edición, auditoría, aprobar/rechazar/re-extraer. Exportación ZIP Marangatu con TXT pipe-separated por tipo de registro. API de exportación genera ZIP en R2 y crea registro Exportacion. UI de exportaciones con historial y descarga. | Flujo completo: upload → extracción → revisión → aprobación → exportación Marangatu. | `web/src/lib/gemini.ts`, `web/src/worker/index.ts`, `web/src/lib/validar.ts`, `web/src/lib/exportar.ts`, `web/src/app/app/comprobantes/[id]/*`, `web/src/app/app/exportaciones/*`, `web/src/app/api/exportaciones/generar/route.ts` | Gemini requiere API Key en Configuración por organización. jszip agregado como dep. | Configurar GEMINI_API_KEY en panel de Configuración para habilitar extracción. |

| 2026-05-19 | **Fix OOM en Fly.io**: `[[vm]]` subido de 256mb a 512mb en ambos procesos (web + worker); `NODE_OPTIONS=--max-old-space-size=450` en `[env]` de fly.toml para acotar el heap de Node y evitar el kill del proceso next-server. | La app se caía con "This page couldn't load" al cargar comprobantes pesados porque exceljs/xml2js/jszip aumentaron el footprint de memoria sobre el límite de 256mb. | `web/fly.toml` | Deploy requiere `fly deploy` manual o push a main. | — |
| 2026-05-19 | **Libro IRP, exclusión e-Kuatia en ZIP, paginación 5.000 filas, validación cliente**: `buildLibroIrpExcel()` con hojas INGRESOS IRP y EGRESOS IRP (filtro `imputaIrpRsp="S"`). `buildZipMara()` ahora pagina en archivos de max 5.000 filas y excluye `origen=E_KUATIA_XML`. Validación client-side en `FormRevision` con `validateForm()` antes de llamar al action. Ruta `/api/exportaciones/libro-irp` nueva. | Cumplimiento de límite técnico SET (5.000 filas/archivo) y decisión D-014 (e-Kuatia no va al ZIP). | `web/src/lib/exportar.ts`, `web/src/lib/exportar-excel.ts`, `web/src/app/api/exportaciones/libro-irp/route.ts`, `web/src/app/app/exportaciones/GenerarLibroIrp.tsx`, `web/src/app/app/comprobantes/[id]/FormRevision.tsx` | — | — |
| 2026-05-19 | **Gestión de usuarios, filtros en comprobantes, ZIP anual**: Página `/app/usuarios` (ADMIN-only) con tabla de usuarios de la org, toggle activo/inactivo, cambio de rol, formulario de invitación. Filtros URL-based en `/app/comprobantes` (estado, búsqueda, desde/hasta). ZIP anual en exportaciones con selector Mensual/Anual. | Los contadores necesitan gestionar acceso del equipo y buscar comprobantes por fecha/estado. | `web/src/app/app/usuarios/`, `web/src/app/app/comprobantes/page.tsx`, `web/src/app/app/exportaciones/GenerarExportacion.tsx`, `web/src/app/api/exportaciones/generar/route.ts`, `web/src/app/app/layout.tsx` | ZIP anual también excluye e-Kuatia (corrección posterior). | — |
| 2026-05-19 | **Extracción directa sin Redis + UX mejorado**: `lib/extraer.ts` extrae la lógica Gemini del worker como módulo compartido. `reextraerComprobante` server action prueba Redis primero; si falla (no configurado), llama `procesarExtraccion()` directamente. `ProcesandoPoller` muestra banner "Extraer con Gemini ahora" tras 12s sin cambio en estado CARGADO. Página de Configuración muestra badges de estado (Gemini key + Redis). | Sin Redis la extracción automática no funcionaba — el comprobante quedaba en CARGADO para siempre. Ahora el contador puede extraer manualmente desde la pantalla del comprobante. | `web/src/lib/extraer.ts` (nuevo), `web/src/worker/index.ts`, `web/src/app/app/comprobantes/[id]/actions.ts`, `web/src/app/app/comprobantes/[id]/ProcesandoPoller.tsx`, `web/src/app/app/configuracion/page.tsx` | La extracción directa puede tardar ~20s y bloquea la request Next.js (maxDuration=60). | Configurar Gemini API key desde /app/configuracion para activar extracción. |

---

## Convenciones del log

- Una fila por cambio significativo. Cambios menores (typos, ajustes
  cosméticos) no se registran aquí.
- Fechas en `YYYY-MM-DD`, zona America/Asuncion.
- Cada fila debe poder leerse fuera de contexto: explicar qué cambió y por qué.
